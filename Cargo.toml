[package]
name = "bdk_wallet"
homepage = "https://bitcoindevkit.org"
version = "3.0.0-alpha.0-p2tsh-0"
repository = "https://github.com/cryptoquick/bdk_wallet"
documentation = "https://docs.rs/bdk_wallet"
description = "A modern, lightweight, descriptor-based wallet library"
keywords = ["bitcoin", "wallet", "descriptor", "psbt"]
readme = "README.md"
license = "MIT OR Apache-2.0"
authors = ["Bitcoin Dev Kit Developers"]
edition = "2021"
rust-version = "1.85.0"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]

bitcoinpqc = { version="0.2.0", features = ["serde"] }

# view configuration for "kellnr-denver-space":
#   cat .cargo/config.toml
bdk_chain = { version = "0.23.2", features = ["miniscript", "serde"], default-features = false, registry = "kellnr-denver-space" }
bitcoin = { version = "0.32.6", features = ["serde", "base64"], default-features = false, registry = "kellnr-denver-space" }
miniscript = { version = "13.0.0", features = ["serde"], default-features = false, registry = "kellnr-denver-space" }

rand_core = { version = "0.6.0" }
serde_json = { version = "1" }
serde = { version = "1", features = ["derive"] }

# Optional dependencies
bdk_core = { version = "0.6.2", optional = true, registry = "kellnr-denver-space" }
bdk_file_store = { version = "0.22.0", optional = true, registry = "kellnr-denver-space" }
anyhow = { version = "1.0.98", optional = true }
bip39 = { version = "2.0", optional = true }
tempfile = { version = "3.20.0", optional = true }

[features]
default = ["std"]
std = ["bitcoin/std", "bitcoin/rand-std", "miniscript/std", "bdk_chain/std"]
compiler = ["miniscript/compiler"]
all-keys = ["keys-bip39"]
keys-bip39 = ["bip39"]
rusqlite = ["bdk_chain/rusqlite"]
file_store = ["bdk_core", "bdk_file_store"]
test-utils = ["std", "anyhow", "tempfile"]

[dev-dependencies]
anyhow = "1"
assert_matches = "1.5.0"
bdk_bitcoind_rpc = { version = "0.22.0", registry = "kellnr-denver-space"  }
bdk_electrum = { version = "0.23.2", registry = "kellnr-denver-space" }
bdk_esplora = { version = "0.22.1", features = ["async-https", "blocking-https", "tokio"], registry = "kellnr-denver-space" }
bdk_wallet = { path = ".", features = ["rusqlite", "file_store", "test-utils"] }
clap = { version = "4.5.17", features = ["derive", "env"] }
ctrlc = "3.4.6"
rand = "0.8"
tempfile = "3"
tokio = { version = "1.38.1", features = ["rt", "rt-multi-thread", "macros"] }

[[example]]
name = "mnemonic_to_descriptors"
path = "examples/mnemonic_to_descriptors.rs"
required-features = ["all-keys"]

[[example]]
name = "miniscriptc"
path = "examples/compiler.rs"
required-features = ["compiler"]

[[example]]
name = "electrum"

[[example]]
name = "esplora_async"

[[example]]
name = "esplora_blocking"

[[example]]
name = "bitcoind_rpc"

[[example]]
name = "p2tsh"
